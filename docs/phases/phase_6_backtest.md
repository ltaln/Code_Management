# Phase 6 · 回测体系预留

## 输入

有效预测档案或真实不存在的查询证据、独立 Prompt、赛果。

## 交付

backtest 流程、evaluation 指标与评价记录。

## 退出条件

有档案零重预测；不存在才历史重建；访问失败不走缺档分支；无赛后泄漏；原预测不变。

## 当前状态

当前为预留设计；不得因评估自动升级模型。

失败处理：保存输入标识、异常、已完成步骤和恢复点；不能把文档生成视为执行验收通过。模型变更仍受冻结规则约束。
