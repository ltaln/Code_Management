# Phase 4 · 数据服务器接口

## 输入

任务范围、数据策略、时间截止。

## 交付

data_engine 的接口与快照合同。

## 退出条件

日期与跨午夜正确；身份错配隔离；事件时点不晚于 as_of；来源可追溯；不混用 latest 快照。

## 当前状态

当前为设计；新接口未接入数据服务器。

失败处理：保存输入标识、异常、已完成步骤和恢复点；不能把文档生成视为执行验收通过。模型变更仍受冻结规则约束。
