# Changelog

## 0.4.15 · 2026-09-06

- 为无法解析的 Actions 请求记录长度、错误位置和尾部十六进制诊断，不记录认证信息或预测正文。

## 0.4.14 · 2026-09-06

- 接受 ChatGPT Actions 传输中偶发的未转义控制字符，避免完整预测批次在保存前被误判为 `INVALID_JSON`。
- HH520 V2.1-Test、模块顺序和预测逻辑保持冻结。

## 0.4.13 · 2026-09-06

命令入口容忍 GPT 偶尔附加的句号或分号，并明确要求 Action 原样传递用户命令，避免精确手机命令在工具调用时被改写。冻结模型未改。

## 0.4.12 · 2026-09-06

全量紧凑提交使用 13 段有序 module_trace、共享证据引用和字符串化结果，减少嵌套 JSON 体积，避免九场一次提交时生成无效请求。冻结模型的模块、顺序和判断规则未改。

## 0.4.11 · 2026-09-06

紧凑预测接口现在强制一次提交全部尚未保存的比赛并立即生成 Prediction Commit；部分批次会被拒绝，防止 GPT 在第一场后提前回复。冻结模型规则未改。

## 0.4.10 · 2026-09-05

并发采集上限提高到 16；只接受带指定日期的权威赛程入口，并统一域名大小写去重，避免无日期首页带入其他日期页面。紧凑预测改为每次保存 1–3 场、最后一块自动生成 Prediction Commit，减少 ChatGPT 在同一回复中的等待和操作次数。冻结模型规则未改。

## 0.4.9 · 2026-09-05

Firecrawl 页面按依赖批次并发采集，默认同时处理 8 页，缩短手机单命令从创建任务到进入 GPT 分析的等待时间。身份核验、完整性门和 HH520 V2.1-Test 冻结逻辑未改。

## 0.4.8 · 2026-09-05

把 GPT 任务状态查询从 25 秒长轮询改为 8 秒短轮询，避免 ChatGPT Actions 在采集期间因单次连接超时而提前结束回复。冻结模型资产和分析逻辑未改。

## 0.1.0 · 2026-09-04

首次实际落地 Code_Management 资产和 Phase 1–6 架构。迁移模型 SOP、原 Prompt、运行增强与回测修订，保存来源 Hash。新增 README、项目记忆、唤醒、接口契约、版本管理和资产检查。HH520 V2.1-Test 核心未重写，Upgrade Package 1 保持 PARKED。自动运行尚未启用。

## 0.2.0 · 2026-09-04

实现持久命令网关、去重、取消、启动检查、租约恢复与报告查询。新增候选 Actions 接口及部署草案。冻结资产及锁未改。真实推理和手机自动回传保持未接通。

## 0.4.7 · 2026-09-05

新增单场紧凑预测保存与紧凑最终确认接口，规避 ChatGPT Actions 对大型请求和响应的出站限制。服务器仍按冻结的 13 模块顺序展开、校验并不可变保存完整报告；模型资产和核心逻辑未改。
冻结资产锁对 Markdown 与 JSON 统一按 LF 换行计算内容哈希，消除 Windows 与 Linux 检出格式造成的误报。
