# Phase 3 · 任务管理中心

## 输入

命令、日期、范围、模型锁定版本。

## 交付

task_manager 的状态机、接口与重试契约。

## 退出条件

任务持久化后返回 task_id；关闭手机不中断；重复事件不重复提交；取消与重启有证据。

## 当前状态

当前为设计；未部署持久 worker。

失败处理：保存输入标识、异常、已完成步骤和恢复点；不能把文档生成视为执行验收通过。模型变更仍受冻结规则约束。
