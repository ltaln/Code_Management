# 规则入口

当前适用规则见 `../models/HH520_V2.1-Test/第十三重要注意事项.md`，原文见 `../docs/sources/`。

规则优先关系：本次用户冻结要求 → 后续明确回测修订 S04 → 仍有效的 S02/S03 补充 → S01 基础模型。S05/S06 提供最终架构，模型内核存在的顺序差异不由架构文件静默覆盖。

身份匹配必须 100%，错配字段排除而保留证据；时间一致性检查必须通过；来源、采集时间、数据 Hash 必须记录。DCS 原始权重及分档见模型运行增强文件。缺失的数值规则不能作为“默认配置”编造。
