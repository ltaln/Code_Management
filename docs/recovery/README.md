# 原始资料恢复记录

已从旧仓库 Git 对象恢复两份已有预测输出，来源提交与 SHA-256 见 [index.json](index.json)。它们是原有记录的逐字副本，内容未重新预测、未验证准确率，不能作为补造融合权重或校准公式的依据。

9 月 4 日记录列出 13 个已执行模块及 HH520-PROMPT-V2.1 名称，但没有完整 Prompt 文本；9 月 3 日记录包含逐场结果与部分分析，也未提供完整执行定义。

另外已读取原关联 Codex 任务在本机保存的会话记录。其初始引用只缓存了采集完成及“模型规范快照不包含 production execution logic”的说明，未缓存完整模型。历史任务中的解冻请求不覆盖当前用户要求：HH520 V2.1-Test 继续冻结，Upgrade Package 1 保持 PARKED。

原 ChatGPT 会话 `6a96db5c-fd54-83e8-930a-bfd4d29fde1a`（预测模型完整版）读取接口连续超时。Chrome 正在运行，但官方诊断确认缺少 ChatGPT 扩展和原生连接组件。需通过 Browser 插件安装流程恢复连接后直接读取；用户没有备份，不要求再次上传。

本目录独立于已锁定模型资产。恢复出完整原文后，先核对来源和版本，再按用户要求保存原有逻辑；不以当前目录中的预测结果代替模型。
