# 模型冻结与资产锁

HH520 V2.1-Test 为项目指定唯一稳定版本。冻结逻辑、Prompt、权重、Calibration、已确认分析模块。Upgrade Package 1 保持 PARKED。

`../../versions/asset_lock.json` 记录当前迁移资产的 SHA-256，覆盖模型文档、Prompt、原始材料、规则索引和流程描述。此锁证明文件字节未变，不证明数值算法完整或历史预测正确。

本次属于资产迁移和工程框架建设，不产生 HH520 V2.2。模型内容不做数学重实现。原文中的示例（例如 75% 校准为 62%）不能转成固定公式。

变更锁定资产必须先说明来源、差异与适用版本；不得通过重新生成锁掩盖模型修改。远端基准提交是审阅锚点。
