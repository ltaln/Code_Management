# Phase 5 · GPT 自动预测

## 输入

合格快照、冻结模型、对应 Prompt、可执行顺序。

## 交付

prediction 的交接接口、模块证据、提交与报告模板。

## 退出条件

所有模块按确认顺序完成或注明原规则允许的降级；结果 Hash 与远端回执一致；用户获得完整输出。

## 当前状态

当前为框架；运行关闭，不实现替代预测算法。

失败处理：保存输入标识、异常、已完成步骤和恢复点；不能把文档生成视为执行验收通过。模型变更仍受冻结规则约束。
